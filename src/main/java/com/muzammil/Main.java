package com.muzammil;

import com.muzammil.model.Author;
import com.muzammil.model.Book;
import com.muzammil.service.AuthorService;
import com.muzammil.service.BookService;


public class Main {
    public static void main(String[] args) {
        AuthorService authorService = new AuthorService();
        BookService bookService = new BookService();
//        Author author = new Author();
//        author.setName("Muzammil");
//        author.getBooks().add(new Book("Crows", author));
//        author.getBooks().add(new Book("GOT", author));
//
//        authorService.save(author);

//        bookService.getBooks().forEach(System.out::println);
//        bookService.getBooksByAuthor(authorService.getAuthor(1)).forEach(System.out::println);
//        bookService.deleteBook(2);
//        bookService.getBooks().forEach(System.out::println);
//        Book book = bookService.getBook(1);
//        System.err.println(book);
//        book.setTitle("Book Title");
//        bookService.updateBook(book);
//        System.out.println(book = bookService.getBook(1));
    }
}